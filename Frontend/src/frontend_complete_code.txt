=========================================
SWIFTb - COMPLETE FRONTEND CODE
Generated: $(date)
=========================================

=========================================
FOLDER STRUCTURE
=========================================
./css/components.css
./css/forgotPassword.css
./css/landingPage.css
./css/loginPage.css
./css/registerPage.css
./css/style.css
./css/variables.css
./index.html
./js/api.js
./js/app.js
./js/components.js
./js/forgotPassword.js
./js/landingPageJScript.js
./js/loginPage.js
./js/registerPage.js
./loginPage.html
./pages/admin/account-types.html
./pages/admin/application-detail.html
./pages/admin/applications.html
./pages/admin/dashboard.html
./pages/admin/reports.html
./pages/admin/users.html
./pages/auth/forgotPassword.html
./pages/auth/loginPage.html
./pages/auth/registerPage.html
./pages/customer/application-status.html
./pages/customer/apply-step1.html
./pages/customer/apply-step2.html
./pages/customer/apply-step3.html
./pages/customer/apply-step4.html
./pages/customer/dashboard.html
./pages/customer/landingPage.html
./pages/customer/profile.html
./pages/shared/sidebar.html
./registerPage.html

=========================================
FILE: index.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>SWIFTb - Open Account Online</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .hero {
      text-align: center;
      padding: 60px 0 40px;
      background: linear-gradient(135deg, var(--primary) 0%, #1a3a6a 100%);
      color: white;
      border-radius: 0 0 30px 30px;
      margin-bottom: var(--space);
    }
    .hero h1 {
      color: white;
      font-size: 3rem;
      font-weight: 800;
    }
    .hero h1 span { color: var(--accent); }
    .hero p {
      color: rgba(255,255,255,0.85);
      max-width: 600px;
      margin: 12px auto 0;
      font-size: 1.1rem;
    }
    .auth-buttons {
      display: flex;
      gap: var(--space);
      justify-content: center;
      margin-top: var(--space);
      flex-wrap: wrap;
    }
    .btn-outline {
      background: transparent;
      color: white;
      border-color: rgba(255,255,255,0.3);
    }
    .btn-outline:hover {
      background: rgba(255,255,255,0.1);
      border-color: rgba(255,255,255,0.5);
    }
    .btn-gold {
      background: var(--accent);
      color: var(--primary);
      border-color: var(--accent);
    }
    .btn-gold:hover {
      background: var(--accent-light);
      border-color: var(--accent-light);
    }
    .features {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: var(--space);
      margin: var(--space) 0;
    }
    .features .item { 
      text-align: center; 
      padding: var(--space); 
      background: white;
      border-radius: var(--radius);
      box-shadow: var(--shadow);
    }
    .features .item .icon { 
      font-size: 2.5rem; 
      color: var(--accent); 
      margin-bottom: 12px;
    }
    .features .item h4 { font-size: 1rem; margin-bottom: 4px; }
    .features .item p { font-size: 0.8rem; color: var(--gray-500); }
    .footer {
      text-align: center;
      padding: var(--space);
      color: var(--gray-500);
      font-size: 0.8rem;
      border-top: 1px solid var(--gray-200);
      margin-top: var(--space);
    }
    .bank-grid {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(240px, 1fr));
      gap: var(--space);
      padding: var(--space) 0;
    }
    .bank-card {
      background: var(--white);
      border: 2px solid var(--gray-200);
      border-radius: var(--radius);
      padding: calc(var(--space) * 1.5);
      text-align: center;
      cursor: pointer;
      transition: all 0.2s;
      box-shadow: var(--shadow);
    }
    .bank-card:hover {
      transform: translateY(-4px);
      border-color: var(--accent);
      box-shadow: var(--shadow-lg);
    }
    .bank-card .icon {
      font-size: 2rem;
      width: 60px;
      height: 60px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      margin: 0 auto var(--space);
      color: var(--white);
    }
  </style>
</head>
<body>
  <!-- Hero Section -->
  <div class="hero">
    <div class="container">
      <h1>SWIFT<span>b</span></h1>
      <p>Open a bank account online in minutes. Choose your bank and start your application.</p>
      
      <div class="auth-buttons">
        <a href="/loginPage.html" class="btn btn-outline">
          <i class="fas fa-sign-in-alt"></i> Login
        </a>
        <a href="/registerPage.html" class="btn btn-gold">
          <i class="fas fa-user-plus"></i> Register
        </a>
        <a href="/pages/customer/dashboard.html" class="btn btn-outline">
          <i class="fas fa-arrow-right"></i> Skip to Demo
        </a>
      </div>
    </div>
  </div>

  <!-- Banks Section -->
  <div class="container">
    <h2 class="text-center">Select Your Bank</h2>
    <p class="text-center text-muted">Choose a bank to begin your account opening process</p>
    <div class="bank-grid" id="bankGrid"></div>
  </div>

  <!-- Features -->
  <div class="container">
    <h2 class="text-center">Why SWIFTb?</h2>
    <p class="text-center text-muted">The fastest way to open a bank account in Malawi</p>
    <div class="features">
      <div class="item">
        <div class="icon"><i class="fas fa-mobile-alt"></i></div>
        <h4>100% Online</h4>
        <p>No branch visit required</p>
      </div>
      <div class="item">
        <div class="icon"><i class="fas fa-shield-alt"></i></div>
        <h4>Secure & Safe</h4>
        <p>Your data is protected</p>
      </div>
      <div class="item">
        <div class="icon"><i class="fas fa-clock"></i></div>
        <h4>5 Minutes</h4>
        <p>Open your account fast</p>
      </div>
      <div class="item">
        <div class="icon"><i class="fas fa-id-card"></i></div>
        <h4>National ID KYC</h4>
        <p>Simple verification</p>
      </div>
    </div>
  </div>

  <div class="container">
    <div class="footer">
      &copy; 2026 SWIFTb - Online Account Opening System
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    const BANKS = [
      { id: 'nbm', name: 'National Bank of Malawi', shortName: 'NBM', icon: 'fa-university', color: '#0A1F44', description: 'Full-service commercial bank', badge: 'Popular' },
      { id: 'fdh', name: 'FDH Bank', shortName: 'FDH', icon: 'fa-building', color: '#003366', description: 'Leading financial services provider', badge: 'Fast' },
      { id: 'standard', name: 'Standard Bank', shortName: 'Standard', icon: 'fa-landmark', color: '#0066b3', description: 'Global banking network' },
      { id: 'ecobank', name: 'Ecobank Malawi', shortName: 'Ecobank', icon: 'fa-globe-africa', color: '#006b3e', description: 'Pan-African banking' },
      { id: 'opportunity', name: 'Opportunity Bank', shortName: 'Opportunity', icon: 'fa-hand-holding-usd', color: '#f57c00', description: 'Microfinance & SME banking', badge: 'SME Focus' },
      { id: 'mybucks', name: 'MyBucks Banking', shortName: 'MyBucks', icon: 'fa-coins', color: '#e65100', description: 'Digital-first banking' },
      { id: 'nbs', name: 'NBS Bank', shortName: 'NBS', icon: 'fa-piggy-bank', color: '#2e7d32', description: 'Community banking' },
      { id: 'first-capital', name: 'First Capital Bank', shortName: 'First Capital', icon: 'fa-chart-line', color: '#b71c1c', description: 'Corporate & personal banking' }
    ];

    function renderBanks() {
      const grid = document.getElementById('bankGrid');
      if (!grid) return;
      grid.innerHTML = BANKS.map(bank => `
        <div class="bank-card" onclick="selectBank('${bank.id}')">
          <div class="icon" style="background:${bank.color};">
            <i class="fas ${bank.icon}"></i>
          </div>
          <h3>${bank.name}</h3>
          <p>${bank.description}</p>
          ${bank.badge ? `<span class="badge badge-approved">${bank.badge}</span>` : ''}
          <div style="margin-top:var(--space);">
            <button class="btn btn-primary btn-sm" onclick="event.stopPropagation();selectBank('${bank.id}')">
              Apply Now <i class="fas fa-arrow-right"></i>
            </button>
          </div>
        </div>
      `).join('');
    }

    function selectBank(bankId) {
      const bank = BANKS.find(b => b.id === bankId);
      if (!bank) return;

      setStorage('selectedBank', bank);
      removeStorage('wizardDraft');
      
      showToast(`Selected ${bank.name}. Starting application...`, 'success');
      setTimeout(() => {
        window.location.href = '/pages/customer/dashboard.html';
      }, 800);
    }

    document.addEventListener('DOMContentLoaded', renderBanks);
  </script>
</body>
</html>

=========================================
FILE: loginPage.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .auth-container {
      max-width: 400px;
      margin: 80px auto;
      padding: var(--space);
    }
    .auth-container .logo {
      text-align: center;
      font-family: 'Poppins', sans-serif;
      font-size: 2rem;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: var(--space);
    }
    .auth-container .logo span { color: var(--accent); }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="logo">SWIFT<span>b</span></div>
    <div class="card">
      <h2 style="text-align:center;">Welcome Back</h2>
      <p class="text-center text-muted">Log in to continue your application</p>

      <form id="loginForm" class="mt-2" onsubmit="handleLogin(event)">
        <div class="input-group">
          <label>Email</label>
          <input type="email" id="email" placeholder="you@example.com" required />
        </div>
        <div class="input-group">
          <label>Password</label>
          <input type="password" id="password" placeholder="••••••••" required />
        </div>

        <button type="submit" class="btn btn-gold btn-block" id="loginBtn">
          <i class="fas fa-sign-in-alt"></i> Log In
        </button>
      </form>

      <p class="text-center mt-2" style="font-size:0.875rem;">
        Don't have an account? <a href="/registerPage.html">Sign up</a>
      </p>

      <hr style="margin:var(--space) 0;" />
      
      <div style="text-align:center;">
        <p class="text-muted" style="font-size:0.75rem;">Quick Demo</p>
        <button class="btn btn-secondary btn-sm" onclick="demoLogin()">
          <i class="fas fa-user"></i> Continue as Guest
        </button>
      </div>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script src="/js/api.js"></script>
  <script>
    async function handleLogin(e) {
      e.preventDefault();
      
      const email = document.getElementById('email').value;
      const password = document.getElementById('password').value;
      const btn = document.getElementById('loginBtn');

      if (!email || !password) {
        showToast('Please fill in all fields.', 'error');
        return;
      }

      // Show loading
      btn.disabled = true;
      btn.innerHTML = '<i class="fas fa-spinner fa-spin"></i> Logging in...';

      try {
        const result = await api.login({ email, password });
        showToast('Login successful!', 'success');
        
        // Store user data
        sessionStorage.setItem('token', result.token);
        sessionStorage.setItem('role', result.role || 'customer');
        sessionStorage.setItem('user', JSON.stringify(result.user));

        // Redirect after short delay
        setTimeout(() => {
          window.location.href = '/pages/customer/dashboard.html';
        }, 1000);
        
      } catch (error) {
        showToast('Login failed: ' + error.message, 'error');
        btn.disabled = false;
        btn.innerHTML = '<i class="fas fa-sign-in-alt"></i> Log In';
      }
    }

    // Check if already logged in
    document.addEventListener('DOMContentLoaded', function() {
      const token = sessionStorage.getItem('token');
      if (token) {
        window.location.href = '/pages/customer/dashboard.html';
      }
    });
  </script>
</body>
</html>

=========================================
FILE: registerPage.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Register - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .auth-container {
      max-width: 400px;
      margin: 40px auto;
      padding: var(--space);
    }
    .auth-container .logo {
      text-align: center;
      font-family: 'Poppins', sans-serif;
      font-size: 2rem;
      font-weight: 700;
      color: var(--primary);
      margin-bottom: var(--space);
    }
    .auth-container .logo span { color: var(--accent); }
  </style>
</head>
<body>
  <div class="auth-container">
    <div class="logo">SWIFT<span>b</span></div>
    <div class="card">
      <h2 style="text-align:center;">Create Account</h2>
      <p class="text-center text-muted">Open your account in minutes</p>

      <form id="registerForm" class="mt-2" onsubmit="handleRegister(event)">
        <div class="input-group">
          <label>Full Name *</label>
          <input type="text" id="fullName" placeholder="John Doe" required />
        </div>
        <div class="input-group">
          <label>Email *</label>
          <input type="email" id="email" placeholder="you@example.com" required />
        </div>
        <div class="input-group">
          <label>Phone Number *</label>
          <input type="tel" id="phone" placeholder="+265 888 123 456" required />
        </div>
        <div class="input-group">
          <label>Password *</label>
          <input type="password" id="password" placeholder="Min 8 characters" required />
        </div>
        <div class="input-group">
          <label>Confirm Password *</label>
          <input type="password" id="confirmPassword" placeholder="Confirm your password" required />
        </div>

        <button type="submit" class="btn btn-gold btn-block">
          <i class="fas fa-user-plus"></i> Create Account
        </button>
      </form>

      <p class="text-center mt-2" style="font-size:0.875rem;">
        Already have an account? <a href="/loginPage.html">Log in</a>
      </p>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script src="/js/api.js"></script>
  <script>
    async function handleRegister(e) {
      e.preventDefault();
      
      const fullName = document.getElementById('fullName').value;
      const email = document.getElementById('email').value;
      const phone = document.getElementById('phone').value;
      const password = document.getElementById('password').value;
      const confirmPassword = document.getElementById('confirmPassword').value;

      if (!fullName || !email || !phone || !password) {
        showToast('Please fill in all required fields.', 'error');
        return;
      }

      if (password !== confirmPassword) {
        showToast('Passwords do not match.', 'error');
        return;
      }

      if (password.length < 8) {
        showToast('Password must be at least 8 characters.', 'error');
        return;
      }

      try {
        await api.register({ fullName, email, phoneNumber: phone, password });
        showToast('Registration successful! Please log in.', 'success');
        setTimeout(() => {
          window.location.href = '/loginPage.html';
        }, 1500);
      } catch (error) {
        showToast('Registration failed: ' + error.message, 'error');
      }
    }
  </script>
</body>
</html>

=========================================
FILE: landingPage.html
=========================================

=========================================
FILE: css/style.css
=========================================
/* ========================================
   SWIFTb - Complete Styles
   ======================================== */

:root {
  --primary: #0A1F44;
  --primary-light: #16407E;
  --accent: #D4AF37;
  --accent-light: #e8c84a;
  --bg: #F8FAFC;
  --text: #1A1F36;
  --white: #FFFFFF;
  --gray-50: #F9FAFB;
  --gray-100: #F3F4F6;
  --gray-200: #E5E7EB;
  --gray-300: #D1D5DB;
  --gray-400: #9CA3AF;
  --gray-500: #6B7280;
  --gray-600: #4B5563;
  --gray-700: #374151;
  --success: #1E7A4B;
  --warning: #A15C1E;
  --danger: #B3261E;
  --radius: 10px;
  --radius-sm: 6px;
  --shadow: 0 2px 12px rgba(10, 31, 68, 0.08);
  --shadow-lg: 0 8px 32px rgba(10, 31, 68, 0.15);
  --space: 16px;
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  font-family: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  background: var(--bg);
  color: var(--text);
  line-height: 1.5;
  min-height: 100vh;
}

h1, h2, h3, h4 {
  font-family: 'Poppins', sans-serif;
  color: var(--primary);
}

a {
  text-decoration: none;
  color: var(--primary-light);
}
a:hover {
  color: var(--accent);
}

/* ---------- Utilities ---------- */
.container {
  max-width: 1100px;
  margin: 0 auto;
  padding: 0 var(--space);
}

.text-center { text-align: center; }
.text-muted { color: var(--gray-500); }
.text-success { color: var(--success); }
.text-warning { color: var(--warning); }
.text-danger { color: var(--danger); }

.mt-1 { margin-top: 8px; }
.mt-2 { margin-top: var(--space); }
.mt-3 { margin-top: calc(var(--space) * 1.5); }
.mt-4 { margin-top: calc(var(--space) * 2); }
.mb-2 { margin-bottom: var(--space); }
.mb-3 { margin-bottom: calc(var(--space) * 1.5); }

.flex { display: flex; }
.flex-col { flex-direction: column; }
.items-center { align-items: center; }
.justify-between { justify-content: space-between; }
.gap-2 { gap: var(--space); }
.gap-3 { gap: calc(var(--space) * 1.5); }
.flex-wrap { flex-wrap: wrap; }
.hidden { display: none !important; }

/* ---------- Buttons ---------- */
.btn {
  display: inline-flex;
  align-items: center;
  gap: 8px;
  padding: 10px 24px;
  font-family: 'Inter', sans-serif;
  font-size: 0.875rem;
  font-weight: 600;
  border: 2px solid transparent;
  border-radius: var(--radius-sm);
  cursor: pointer;
  transition: all 0.2s;
  text-decoration: none;
  min-height: 44px;
  background: var(--white);
}

.btn-primary {
  background: var(--primary);
  color: var(--white);
  border-color: var(--primary);
}
.btn-primary:hover {
  background: var(--primary-light);
  border-color: var(--primary-light);
  transform: translateY(-1px);
  box-shadow: var(--shadow);
}

.btn-gold {
  background: var(--accent);
  color: var(--primary);
  border-color: var(--accent);
}
.btn-gold:hover {
  background: var(--accent-light);
  border-color: var(--accent-light);
  transform: translateY(-1px);
  box-shadow: var(--shadow);
}

.btn-secondary {
  background: transparent;
  color: var(--primary);
  border-color: var(--gray-300);
}
.btn-secondary:hover {
  background: var(--gray-100);
  border-color: var(--gray-400);
}

.btn-danger {
  background: var(--danger);
  color: var(--white);
  border-color: var(--danger);
}
.btn-danger:hover {
  background: #9a1e18;
  border-color: #9a1e18;
}

.btn-sm {
  padding: 6px 16px;
  font-size: 0.75rem;
  min-height: 32px;
}
.btn-lg {
  padding: 14px 32px;
  font-size: 1rem;
  min-height: 52px;
}
.btn-block {
  width: 100%;
  justify-content: center;
}
.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  transform: none !important;
}

/* ---------- Cards ---------- */
.card {
  background: var(--white);
  border: 1px solid var(--gray-200);
  border-radius: var(--radius);
  padding: calc(var(--space) * 1.5);
  box-shadow: var(--shadow);
  transition: all 0.2s;
}
.card:hover {
  box-shadow: var(--shadow-lg);
}
.card-header {
  font-weight: 600;
  font-size: 1.125rem;
  color: var(--primary);
  margin-bottom: var(--space);
}

/* ---------- Inputs ---------- */
.input-group {
  margin-bottom: var(--space);
}
.input-group label {
  display: block;
  font-size: 0.875rem;
  font-weight: 500;
  color: var(--gray-700);
  margin-bottom: 4px;
}
.input-group input,
.input-group select,
.input-group textarea {
  width: 100%;
  padding: 10px 14px;
  font-family: 'Inter', sans-serif;
  font-size: 0.875rem;
  border: 2px solid var(--gray-200);
  border-radius: var(--radius-sm);
  transition: all 0.2s;
  background: var(--white);
  color: var(--text);
}
.input-group input:focus,
.input-group select:focus,
.input-group textarea:focus {
  outline: none;
  border-color: var(--primary-light);
  box-shadow: 0 0 0 4px rgba(22, 64, 126, 0.1);
}
.input-group input.error {
  border-color: var(--danger);
}
.input-group .error-text {
  color: var(--danger);
  font-size: 0.75rem;
  margin-top: 4px;
  display: none;
}
.input-group .error-text.show {
  display: block;
}

/* ---------- Badges ---------- */
.badge {
  display: inline-flex;
  padding: 4px 12px;
  border-radius: 20px;
  font-size: 0.75rem;
  font-weight: 600;
  text-transform: capitalize;
}
.badge-draft { background: var(--gray-200); color: var(--gray-700); }
.badge-pending { background: #fef3c7; color: var(--warning); }
.badge-approved { background: #d1fae5; color: var(--success); }
.badge-rejected { background: #fee2e2; color: var(--danger); }
.badge-resubmission { background: #fef3c7; color: var(--warning); border: 2px solid var(--warning); }

/* ---------- Stepper ---------- */
.stepper {
  display: flex;
  align-items: center;
  justify-content: space-between;
  margin-bottom: var(--space);
  padding: var(--space) 0;
  position: relative;
}
.stepper::before {
  content: '';
  position: absolute;
  top: 50%;
  left: 10%;
  right: 10%;
  height: 3px;
  background: var(--gray-200);
  transform: translateY(-50%);
  z-index: 0;
}
.stepper .step {
  display: flex;
  flex-direction: column;
  align-items: center;
  position: relative;
  z-index: 1;
}
.stepper .step .circle {
  width: 40px;
  height: 40px;
  border-radius: 50%;
  background: var(--gray-200);
  color: var(--gray-500);
  display: flex;
  align-items: center;
  justify-content: center;
  font-weight: 700;
  font-size: 0.875rem;
  transition: all 0.2s;
}
.stepper .step.active .circle {
  background: var(--primary);
  color: var(--white);
  box-shadow: 0 0 0 4px rgba(10, 31, 68, 0.15);
}
.stepper .step.completed .circle {
  background: var(--success);
  color: var(--white);
}
.stepper .step .label {
  margin-top: 8px;
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--gray-500);
}
.stepper .step.active .label {
  color: var(--primary);
  font-weight: 600;
}

/* ---------- Toast ---------- */
#toast-container {
  position: fixed;
  top: var(--space);
  right: var(--space);
  z-index: 9999;
  display: flex;
  flex-direction: column;
  gap: 8px;
  max-width: 400px;
  width: 100%;
}
.toast {
  padding: 14px 20px;
  border-radius: var(--radius-sm);
  color: var(--white);
  font-weight: 500;
  font-size: 0.875rem;
  box-shadow: var(--shadow-lg);
  animation: slideIn 0.3s ease-out;
  display: flex;
  align-items: center;
  gap: var(--space);
}
@keyframes slideIn {
  from { opacity: 0; transform: translateX(100%); }
  to { opacity: 1; transform: translateX(0); }
}
.toast-success { background: var(--success); }
.toast-error { background: var(--danger); }
.toast-warning { background: var(--warning); }
.toast-info { background: var(--primary-light); }
.toast .toast-close {
  background: none;
  border: none;
  color: var(--white);
  font-size: 1.25rem;
  cursor: pointer;
  margin-left: auto;
  opacity: 0.7;
}
.toast .toast-close:hover { opacity: 1; }

/* ---------- Upload ---------- */
.upload-zone {
  border: 2px dashed var(--gray-300);
  border-radius: var(--radius);
  padding: calc(var(--space) * 2);
  text-align: center;
  cursor: pointer;
  transition: all 0.2s;
  background: var(--gray-50);
}
.upload-zone:hover,
.upload-zone.dragover {
  border-color: var(--primary-light);
  background: rgba(22, 64, 126, 0.05);
}
.upload-zone .icon {
  font-size: 3rem;
  color: var(--gray-400);
}
.upload-zone .text {
  margin-top: 8px;
  font-weight: 500;
  color: var(--gray-600);
}
.upload-preview {
  margin-top: var(--space);
  display: flex;
  align-items: center;
  gap: var(--space);
  padding: var(--space);
  background: var(--gray-50);
  border-radius: var(--radius-sm);
}
.upload-preview img {
  max-width: 100px;
  max-height: 60px;
  border-radius: var(--radius-sm);
  object-fit: cover;
}
.upload-preview .file-info {
  flex: 1;
  font-size: 0.875rem;
}
.upload-preview .file-name {
  font-weight: 500;
}
.upload-preview .file-size {
  color: var(--gray-500);
  font-size: 0.75rem;
}
.progress-bar {
  width: 100%;
  height: 6px;
  background: var(--gray-200);
  border-radius: 20px;
  overflow: hidden;
  margin-top: var(--space);
  display: none;
}
.progress-bar .progress-fill {
  height: 100%;
  background: var(--primary-light);
  border-radius: 20px;
  transition: width 0.3s;
  width: 0%;
}
.progress-bar.active { display: block; }

/* ---------- Responsive ---------- */
@media (max-width: 768px) {
  .stepper { flex-direction: column; gap: 8px; }
  .stepper::before { display: none; }
  .stepper .step { flex-direction: row; width: 100%; gap: var(--space); }
  .stepper .step .circle { width: 32px; height: 32px; font-size: 0.75rem; }
  .card { padding: var(--space); }
  .flex { flex-wrap: wrap; }
}

=========================================
FILE: js/app.js
=========================================
/* ========================================
   SWIFTb - Application Logic
   ======================================== */

// ---------- Toast System ----------
function showToast(message, type = 'info', duration = 4000) {
  const container = document.getElementById('toast-container') || (() => {
    const el = document.createElement('div');
    el.id = 'toast-container';
    el.className = 'toast-container';
    document.body.appendChild(el);
    return el;
  })();

  const toast = document.createElement('div');
  toast.className = `toast toast-${type}`;
  toast.innerHTML = `
    <span>${message}</span>
    <button class="toast-close" onclick="this.parentElement.remove()">&times;</button>
  `;
  container.appendChild(toast);

  setTimeout(() => {
    toast.style.opacity = '0';
    toast.style.transform = 'translateX(100%)';
    setTimeout(() => toast.remove(), 300);
  }, duration);
}

// ---------- Storage Helpers ----------
function setStorage(key, value) {
  sessionStorage.setItem(key, JSON.stringify(value));
}

function getStorage(key) {
  try {
    return JSON.parse(sessionStorage.getItem(key));
  } catch {
    return sessionStorage.getItem(key);
  }
}

function removeStorage(key) {
  sessionStorage.removeItem(key);
}

function getUserData() {
  return getStorage('user') || {};
}

function getSelectedBank() {
  return getStorage('selectedBank');
}

// ---------- Wizard State ----------
const wizard = {
  data: null,

  load() {
    this.data = getStorage('wizardDraft') || this.getDefault();
    return this.data;
  },

  getDefault() {
    return {
      step: 1,
      accountType: null,
      accountTypeId: null,
      personalDetails: {
        address: '',
        occupation: '',
        incomeRange: '',
        nextOfKin: '',
        nextOfKinPhone: '',
      },
      nationalId: {
        number: '',
        file: null,
        fileUrl: null,
        uploaded: false,
        documentId: null,
      },
      termsAccepted: false,
      submitted: false,
      applicationId: null,
    };
  },

  save() {
    setStorage('wizardDraft', this.data);
  },

  reset() {
    this.data = this.getDefault();
    this.save();
  },

  setAccountType(id, name) {
    this.data.accountTypeId = id;
    this.data.accountType = name;
    this.save();
  },

  setPersonalDetails(details) {
    this.data.personalDetails = { ...this.data.personalDetails, ...details };
    this.save();
  },

  setNationalId(number, file, fileUrl) {
    this.data.nationalId.number = number;
    if (file) this.data.nationalId.file = file;
    if (fileUrl) this.data.nationalId.fileUrl = fileUrl;
    this.save();
  },

  setNationalIdUploaded(documentId) {
    this.data.nationalId.uploaded = true;
    this.data.nationalId.documentId = documentId;
    this.save();
  },

  setTermsAccepted(accepted) {
    this.data.termsAccepted = accepted;
    this.save();
  },

  setApplicationId(id) {
    this.data.applicationId = id;
    this.save();
  },

  getFullData() {
    const user = getUserData();
    return {
      accountTypeId: this.data.accountTypeId,
      address: this.data.personalDetails.address || user.address || '',
      occupation: this.data.personalDetails.occupation || user.occupation || '',
      incomeRange: this.data.personalDetails.incomeRange || '',
      nextOfKin: this.data.personalDetails.nextOfKin || '',
      nextOfKinPhone: this.data.personalDetails.nextOfKinPhone || '',
      nationalId: this.data.nationalId.number || user.nationalId || '',
      documentId: this.data.nationalId.documentId,
      termsAccepted: this.data.termsAccepted,
    };
  },

  isComplete() {
    const d = this.data;
    const hasAccountType = d.accountTypeId !== null && d.accountTypeId !== undefined;
    const hasAddress = d.personalDetails.address && d.personalDetails.address.trim() !== '';
    const hasOccupation = d.personalDetails.occupation && d.personalDetails.occupation.trim() !== '';
    const hasIdNumber = d.nationalId.number && d.nationalId.number.trim() !== '';
    const hasIdUploaded = d.nationalId.uploaded === true;
    const hasTerms = d.termsAccepted === true;
    
    return hasAccountType && hasAddress && hasOccupation && hasIdNumber && hasIdUploaded && hasTerms;
  }
};

// ---------- Bank Data ----------
const BANKS = [
  { id: 'nbm', name: 'National Bank of Malawi', shortName: 'NBM', icon: 'fa-university', color: '#0A1F44', description: 'Full-service commercial bank', badge: 'Popular' },
  { id: 'fdh', name: 'FDH Bank', shortName: 'FDH', icon: 'fa-building', color: '#003366', description: 'Leading financial services provider', badge: 'Fast' },
  { id: 'standard', name: 'Standard Bank', shortName: 'Standard', icon: 'fa-landmark', color: '#0066b3', description: 'Global banking network' },
  { id: 'ecobank', name: 'Ecobank Malawi', shortName: 'Ecobank', icon: 'fa-globe-africa', color: '#006b3e', description: 'Pan-African banking' },
  { id: 'opportunity', name: 'Opportunity Bank', shortName: 'Opportunity', icon: 'fa-hand-holding-usd', color: '#f57c00', description: 'Microfinance & SME banking', badge: 'SME Focus' },
  { id: 'mybucks', name: 'MyBucks Banking', shortName: 'MyBucks', icon: 'fa-coins', color: '#e65100', description: 'Digital-first banking' },
  { id: 'nbs', name: 'NBS Bank', shortName: 'NBS', icon: 'fa-piggy-bank', color: '#2e7d32', description: 'Community banking' },
  { id: 'first-capital', name: 'First Capital Bank', shortName: 'First Capital', icon: 'fa-chart-line', color: '#b71c1c', description: 'Corporate & personal banking' }
];

// ---------- Format Helpers ----------
function formatDate(dateStr) {
  if (!dateStr) return '';
  const d = new Date(dateStr);
  return d.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric' });
}

function formatDateTime(dateStr) {
  if (!dateStr) return '';
  const d = new Date(dateStr);
  return d.toLocaleDateString('en-US', { year: 'numeric', month: 'short', day: 'numeric', hour: '2-digit', minute: '2-digit' });
}

// ---------- Expose Globally ----------
window.showToast = showToast;
window.setStorage = setStorage;
window.getStorage = getStorage;
window.removeStorage = removeStorage;
window.getUserData = getUserData;
window.getSelectedBank = getSelectedBank;
window.wizard = wizard;
window.BANKS = BANKS;
window.formatDate = formatDate;
window.formatDateTime = formatDateTime;
window.demoLogin = demoLogin;

// ---------- Demo Login Function ----------
function demoLogin() {
  const user = {
    id: 'demo-' + Date.now(),
    fullName: 'Guest User',
    email: 'guest@example.com',
    phoneNumber: '+265 888 123 456',
    role: 'customer'
  };
  
  sessionStorage.setItem('token', 'demo-token-' + Date.now());
  sessionStorage.setItem('role', 'customer');
  sessionStorage.setItem('user', JSON.stringify(user));
  
  showToast('Continuing as guest...', 'success');
  setTimeout(() => {
    window.location.href = '/pages/customer/dashboard.html';
  }, 800);
}

// ---------- Demo Login Function ----------
function demoLogin() {
  const user = {
    id: 'demo-' + Date.now(),
    fullName: 'Guest User',
    email: 'guest@example.com',
    phoneNumber: '+265 888 123 456',
    role: 'customer'
  };
  
  sessionStorage.setItem('token', 'demo-token-' + Date.now());
  sessionStorage.setItem('role', 'customer');
  sessionStorage.setItem('user', JSON.stringify(user));
  
  showToast('Continuing as guest...', 'success');
  setTimeout(() => {
    window.location.href = '/pages/customer/dashboard.html';
  }, 800);
}

// Make sure demoLogin is exposed globally
window.demoLogin = demoLogin;

=========================================
FILE: js/api.js
=========================================
/* ========================================
   SWIFTb - API Integration
   ======================================== */

const API_BASE = 'http://localhost:3000/api';

class ApiClient {
  constructor() {
    this.baseURL = API_BASE;
  }

  getToken() {
    return sessionStorage.getItem('token');
  }

  async request(endpoint, options = {}) {
    const token = this.getToken();
    const headers = {
      'Content-Type': 'application/json',
      ...options.headers,
    };

    if (token) {
      headers['Authorization'] = `Bearer ${token}`;
    }

    if (options.body && options.body instanceof FormData) {
      delete headers['Content-Type'];
    }

    try {
      const response = await fetch(`${this.baseURL}${endpoint}`, {
        ...options,
        headers,
      });

      if (response.status === 401) {
        sessionStorage.removeItem('token');
        sessionStorage.removeItem('role');
        sessionStorage.removeItem('user');
        if (!window.location.pathname.includes('loginPage') && 
            !window.location.pathname.includes('registerPage')) {
          window.location.href = '/loginPage.html';
        }
        throw new Error('Session expired. Please login again.');
      }

      const data = await response.json();

      if (!response.ok) {
        throw new Error(data.message || 'Something went wrong');
      }

      return data;
    } catch (error) {
      console.error('API Error:', error);
      throw error;
    }
  }

  // ========================================
  // AUTH ENDPOINTS
  // ========================================

  async register(userData) {
    return this.request('/auth/register', {
      method: 'POST',
      body: JSON.stringify(userData),
    });
  }

  async login(credentials) {
    const data = await this.request('/auth/login', {
      method: 'POST',
      body: JSON.stringify(credentials),
    });

    if (data.token) {
      sessionStorage.setItem('token', data.token);
      sessionStorage.setItem('role', data.role || 'customer');
      sessionStorage.setItem('user', JSON.stringify(data.user));
    }

    return data;
  }

  // ========================================
  // CUSTOMER ENDPOINTS
  // ========================================

  async getProfile() {
    return this.request('/customers/profile');
  }

  async updateProfile(data) {
    return this.request('/customers/profile', {
      method: 'PUT',
      body: JSON.stringify(data),
    });
  }

  // ========================================
  // APPLICATION ENDPOINTS
  // ========================================

  async createApplication(data) {
    return this.request('/applications', {
      method: 'POST',
      body: JSON.stringify(data),
    });
  }

  async getMyApplications() {
    return this.request('/applications/my');
  }

  async getApplicationById(id) {
    return this.request(`/applications/${id}`);
  }

  // ========================================
  // DOCUMENT ENDPOINTS
  // ========================================

  async uploadDocument(formData) {
    return this.request('/documents/upload', {
      method: 'POST',
      body: formData,
    });
  }

  async getMyDocuments() {
    return this.request('/documents/customer/me');
  }

  // ========================================
  // ACCOUNT TYPE ENDPOINTS
  // ========================================

  async getAccountTypes() {
    return this.request('/account-types');
  }
}

const api = new ApiClient();
window.api = api;

=========================================
FILE: pages/customer/application-status.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Application Status - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/dashboard.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">Application Status</h1>
      <button class="btn btn-secondary btn-sm" style="margin-left:auto;" onclick="loadStatus()">
        <i class="fas fa-sync"></i> Refresh
      </button>
    </div>

    <div class="card" id="statusCard">
      <div id="statusContent"><p class="text-muted">Loading...</p></div>
    </div>

    <div class="card mt-2" id="timelineCard">
      <h3>Application Timeline</h3>
      <div id="timelineContent"><p class="text-muted">No timeline available.</p></div>
    </div>

    <div class="mt-2">
      <a href="/pages/customer/apply-step1.html" class="btn btn-gold">
        <i class="fas fa-plus"></i> New Application
      </a>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', loadStatus);

    function loadStatus() {
      const content = document.getElementById('statusContent');
      content.innerHTML = '<p class="text-muted">Loading...</p>';

      const apps = getStorage('applications') || [];
      const app = apps.length > 0 ? apps[apps.length - 1] : null;

      if (!app) {
        content.innerHTML = `
          <p class="text-muted">You haven't submitted an application yet.</p>
          <a href="/pages/customer/apply-step1.html" class="btn btn-gold btn-sm mt-2">Start Application</a>
        `;
        document.getElementById('timelineContent').innerHTML = '<p class="text-muted">No timeline available.</p>';
        return;
      }

      const statusMap = {
        draft: 'Draft',
        pending: 'Pending Review',
        approved: 'Approved ✅',
        rejected: 'Rejected ❌',
        requires_information: 'Resubmission Requested'
      };
      const badgeClass = {
        draft: 'badge-draft',
        pending: 'badge-pending',
        approved: 'badge-approved',
        rejected: 'badge-rejected',
        requires_information: 'badge-resubmission'
      };

      const status = app.status || 'pending';
      content.innerHTML = `
        <div>
          <p><strong>Application ID:</strong> #${app.id}</p>
          <p><strong>Bank:</strong> ${app.bank || 'Not specified'}</p>
          <p><strong>Account Type:</strong> ${app.accountType || '—'}</p>
          <p><strong>Status:</strong> 
            <span class="badge ${badgeClass[status] || 'badge-pending'}">${statusMap[status] || status}</span>
          </p>
          ${app.submittedDate ? `<p><strong>Submitted:</strong> ${formatDate(app.submittedDate)}</p>` : ''}
        </div>
      `;

      // Timeline
      let timelineHtml = '<ul style="list-style:none;padding:0;">';
      if (app.submittedDate) {
        timelineHtml += `
          <li style="padding:8px 0;border-bottom:1px solid var(--gray-100);">
            <i class="fas fa-file-invoice" style="color:var(--primary);width:24px;"></i>
            <strong>Application Submitted</strong>
            <span style="float:right;color:var(--gray-500);font-size:0.75rem;">${formatDateTime(app.submittedDate)}</span>
          </li>
        `;
      }
      if (app.status === 'approved' || app.status === 'rejected') {
        timelineHtml += `
          <li style="padding:8px 0;border-bottom:1px solid var(--gray-100);">
            <i class="fas ${app.status === 'approved' ? 'fa-check-circle' : 'fa-times-circle'}" style="color:${app.status === 'approved' ? 'var(--success)' : 'var(--danger)'};width:24px;"></i>
            <strong>Application ${app.status === 'approved' ? 'Approved' : 'Rejected'}</strong>
            <span style="float:right;color:var(--gray-500);font-size:0.75rem;">${formatDateTime(new Date().toISOString())}</span>
          </li>
        `;
      }
      timelineHtml += '</ul>';
      document.getElementById('timelineContent').innerHTML = timelineHtml;
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/apply-step1.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Step 1 - Account Type - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .bank-banner {
      background: var(--gray-50);
      border: 2px solid var(--accent);
      border-radius: var(--radius);
      padding: var(--space);
      display: flex;
      align-items: center;
      gap: var(--space);
      margin-bottom: var(--space);
    }
    .bank-banner .icon {
      font-size: 1.8rem;
      width: 50px;
      height: 50px;
      border-radius: 50%;
      display: flex;
      align-items: center;
      justify-content: center;
      background: var(--accent);
      color: var(--white);
    }
    .account-option {
      cursor: pointer;
      flex: 1;
      min-width: 180px;
      transition: all 0.2s;
    }
    .account-option:hover { border-color: var(--accent); }
    .account-option.selected {
      border-color: var(--accent);
      background: rgba(212, 175, 55, 0.05);
    }
  </style>
</head>
<body>
  <div class="container" style="max-width:800px;">
    <!-- Header -->
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/dashboard.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">Apply for Account</h1>
    </div>

    <!-- Bank Banner -->
    <div class="bank-banner" id="bankBanner">
      <div class="icon"><i class="fas fa-university"></i></div>
      <div>
        <div style="font-weight:600;" id="bankName">No bank selected</div>
        <div style="font-size:0.8rem;color:var(--gray-500);">You are applying with this bank</div>
      </div>
      <a href="/" style="margin-left:auto;font-size:0.875rem;">Change</a>
    </div>

    <!-- Stepper -->
    <div class="stepper">
      <div class="step active"><div class="circle">1</div><span class="label">Account Type</span></div>
      <div class="step"><div class="circle">2</div><span class="label">Personal Details</span></div>
      <div class="step"><div class="circle">3</div><span class="label">Upload ID</span></div>
      <div class="step"><div class="circle">4</div><span class="label">Review & Submit</span></div>
    </div>

    <!-- Content -->
    <div class="card">
      <h2>Select Account Type</h2>
      <p class="text-muted">Choose the account that best fits your needs.</p>

      <div class="flex gap-2 mt-2 flex-wrap" id="accountOptions">
        <div class="card account-option" data-id="1" data-name="Savings" onclick="selectAccount(this)">
          <h3>Savings Account</h3>
          <p style="font-size:0.8rem;">Build savings with interest. Min balance: 10,000 MWK</p>
        </div>
        <div class="card account-option" data-id="2" data-name="Current" onclick="selectAccount(this)">
          <h3>Current Account</h3>
          <p style="font-size:0.8rem;">Everyday banking with cheque book access.</p>
        </div>
        <div class="card account-option" data-id="3" data-name="Fixed Deposit" onclick="selectAccount(this)">
          <h3>Fixed Deposit</h3>
          <p style="font-size:0.8rem;">Higher interest for long-term savings.</p>
        </div>
      </div>

      <div id="selectedDisplay" class="mt-2 hidden">
        <p><strong>Selected:</strong> <span id="selectedName"></span></p>
      </div>

      <div style="display:flex;justify-content:space-between;margin-top:var(--space);">
        <a href="/pages/customer/dashboard.html" class="btn btn-secondary">Cancel</a>
        <button id="nextBtn" class="btn btn-gold" disabled onclick="goNext()">
          Next <i class="fas fa-arrow-right"></i>
        </button>
      </div>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      const bank = getSelectedBank();
      if (!bank) {
        showToast('Please select a bank first', 'warning');
        setTimeout(() => window.location.href = '/', 1000);
        return;
      }
      document.getElementById('bankName').textContent = bank.name;
      document.querySelector('.bank-banner .icon i').className = `fas ${bank.icon}`;

      wizard.load();
      // Restore selection
      if (wizard.data.accountTypeId) {
        document.querySelectorAll('.account-option').forEach(el => {
          if (el.dataset.id == wizard.data.accountTypeId) {
            selectAccount(el);
          }
        });
      }
    });

    function selectAccount(el) {
      document.querySelectorAll('.account-option').forEach(opt => {
        opt.classList.remove('selected');
      });
      el.classList.add('selected');

      const id = parseInt(el.dataset.id);
      const name = el.dataset.name;
      wizard.setAccountType(id, name);

      document.getElementById('selectedName').textContent = name;
      document.getElementById('selectedDisplay').classList.remove('hidden');
      document.getElementById('nextBtn').disabled = false;
    }

    function goNext() {
      if (wizard.data.accountTypeId) {
        window.location.href = '/pages/customer/apply-step2.html';
      }
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/apply-step2.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Step 2 - Personal Details - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/apply-step1.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">Apply for Account</h1>
    </div>

    <!-- Stepper -->
    <div class="stepper">
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Account Type</span></div>
      <div class="step active"><div class="circle">2</div><span class="label">Personal Details</span></div>
      <div class="step"><div class="circle">3</div><span class="label">Upload ID</span></div>
      <div class="step"><div class="circle">4</div><span class="label">Review & Submit</span></div>
    </div>

    <div class="card">
      <h2>Personal Details</h2>
      <p class="text-muted">Please provide your personal information.</p>

      <form id="detailsForm" class="mt-2" onsubmit="event.preventDefault(); saveAndNext();">
        <div class="input-group">
          <label>Address *</label>
          <input type="text" id="address" required />
        </div>
        <div class="input-group">
          <label>Occupation *</label>
          <input type="text" id="occupation" required />
        </div>
        <div class="input-group">
          <label>Income Range</label>
          <select id="incomeRange">
            <option value="">Select income range</option>
            <option value="0-50k">0 - 50,000 MWK</option>
            <option value="50k-100k">50,000 - 100,000 MWK</option>
            <option value="100k-250k">100,000 - 250,000 MWK</option>
            <option value="250k-500k">250,000 - 500,000 MWK</option>
            <option value="500k+">500,000+ MWK</option>
          </select>
        </div>
        <div class="input-group">
          <label>Next of Kin</label>
          <input type="text" id="nextOfKin" placeholder="Full name" />
        </div>
        <div class="input-group">
          <label>Next of Kin Phone</label>
          <input type="tel" id="nextOfKinPhone" placeholder="Phone number" />
        </div>

        <div style="display:flex;justify-content:space-between;margin-top:var(--space);">
          <a href="/pages/customer/apply-step1.html" class="btn btn-secondary">
            <i class="fas fa-arrow-left"></i> Back
          </a>
          <button type="submit" class="btn btn-gold">
            Next <i class="fas fa-arrow-right"></i>
          </button>
        </div>
      </form>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      wizard.load();
      const d = wizard.data.personalDetails;
      if (d.address) document.getElementById('address').value = d.address;
      if (d.occupation) document.getElementById('occupation').value = d.occupation;
      if (d.incomeRange) document.getElementById('incomeRange').value = d.incomeRange;
      if (d.nextOfKin) document.getElementById('nextOfKin').value = d.nextOfKin;
      if (d.nextOfKinPhone) document.getElementById('nextOfKinPhone').value = d.nextOfKinPhone;
    });

    function saveAndNext() {
      const details = {
        address: document.getElementById('address').value,
        occupation: document.getElementById('occupation').value,
        incomeRange: document.getElementById('incomeRange').value,
        nextOfKin: document.getElementById('nextOfKin').value,
        nextOfKinPhone: document.getElementById('nextOfKinPhone').value,
      };
      
      // Validate required fields
      if (!details.address || !details.occupation) {
        showToast('Please fill in all required fields.', 'error');
        return;
      }
      
      wizard.setPersonalDetails(details);
      window.location.href = '/pages/customer/apply-step3.html';
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/apply-step3.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Step 3 - Upload ID - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/apply-step2.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">Apply for Account</h1>
    </div>

    <!-- Stepper -->
    <div class="stepper">
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Account Type</span></div>
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Personal Details</span></div>
      <div class="step active"><div class="circle">3</div><span class="label">Upload ID</span></div>
      <div class="step"><div class="circle">4</div><span class="label">Review & Submit</span></div>
    </div>

    <div class="card">
      <h2>Upload National ID</h2>
      <p class="text-muted">Upload a clear image of your National ID. Accepted: JPG, PNG, PDF (max 5MB).</p>

      <div id="uploadZone" class="upload-zone" onclick="document.getElementById('fileInput').click()">
        <div class="icon"><i class="fas fa-cloud-upload-alt"></i></div>
        <div class="text">Drag & drop your National ID here, or click to browse</div>
        <input type="file" id="fileInput" accept="image/*,.pdf" style="display:none;" onchange="handleFile(event)" />
      </div>

      <div id="previewContainer" class="hidden mt-2">
        <div class="upload-preview">
          <img id="previewImage" src="" alt="ID Preview" />
          <div class="file-info">
            <div class="file-name" id="fileName">file.pdf</div>
            <div class="file-size" id="fileSize">0 MB</div>
          </div>
          <button class="btn btn-danger btn-sm" onclick="removeFile()">Remove</button>
        </div>
        <div class="progress-bar" id="progressBar">
          <div class="progress-fill" id="progressFill"></div>
        </div>
      </div>

      <div class="input-group mt-2">
        <label>National ID Number *</label>
        <input type="text" id="idNumber" placeholder="Enter your ID number" />
        <div class="error-text" id="idError">Please enter a valid ID number</div>
      </div>

      <!-- Upload Status Message -->
      <div id="uploadStatus" class="mt-2"></div>

      <div style="display:flex;justify-content:space-between;margin-top:var(--space);">
        <a href="/pages/customer/apply-step2.html" class="btn btn-secondary">
          <i class="fas fa-arrow-left"></i> Back
        </a>
        <button id="uploadBtn" class="btn btn-gold" onclick="uploadAndNext()">
          Upload & Continue <i class="fas fa-arrow-right"></i>
        </button>
      </div>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    let selectedFile = null;
    let isUploading = false;

    document.addEventListener('DOMContentLoaded', () => {
      wizard.load();
      const data = wizard.data.nationalId;
      
      // Restore ID number
      if (data.number) {
        document.getElementById('idNumber').value = data.number;
      }
      
      // Restore uploaded file
      if (data.fileUrl && data.uploaded) {
        document.getElementById('previewImage').src = data.fileUrl;
        document.getElementById('fileName').textContent = 'ID uploaded successfully ✅';
        document.getElementById('previewContainer').classList.remove('hidden');
        document.getElementById('uploadStatus').innerHTML = 
          '<span style="color:var(--success);font-weight:500;">✅ ID already uploaded</span>';
        document.getElementById('uploadBtn').innerHTML = 'Continue <i class="fas fa-arrow-right"></i>';
      }
      
      setupDragDrop();
    });

    function setupDragDrop() {
      const zone = document.getElementById('uploadZone');
      zone.addEventListener('dragover', e => { 
        e.preventDefault(); 
        zone.classList.add('dragover'); 
      });
      zone.addEventListener('dragleave', () => zone.classList.remove('dragover'));
      zone.addEventListener('drop', e => {
        e.preventDefault();
        zone.classList.remove('dragover');
        if (e.dataTransfer.files.length) {
          processFile(e.dataTransfer.files[0]);
        }
      });
    }

    function handleFile(e) {
      if (e.target.files.length) {
        processFile(e.target.files[0]);
      }
    }

    function processFile(file) {
      const validTypes = ['image/jpeg', 'image/png', 'image/jpg', 'application/pdf'];
      
      if (!validTypes.includes(file.type)) {
        showToast('Please upload JPG, PNG, or PDF files only.', 'error');
        return;
      }
      
      if (file.size > 5 * 1024 * 1024) {
        showToast('File size must be less than 5MB.', 'error');
        return;
      }

      selectedFile = file;
      
      // Reset upload status
      document.getElementById('uploadStatus').innerHTML = '';
      document.getElementById('uploadBtn').innerHTML = 'Upload & Continue <i class="fas fa-arrow-right"></i>';

      // Show preview
      const reader = new FileReader();
      reader.onload = (e) => {
        document.getElementById('previewImage').src = e.target.result;
        document.getElementById('fileName').textContent = file.name;
        document.getElementById('fileSize').textContent = (file.size / 1024 / 1024).toFixed(2) + ' MB';
        document.getElementById('previewContainer').classList.remove('hidden');
      };
      
      if (file.type === 'application/pdf') {
        document.getElementById('previewImage').src = 'https://via.placeholder.com/100x60?text=PDF';
        document.getElementById('previewContainer').classList.remove('hidden');
        document.getElementById('fileName').textContent = file.name;
        document.getElementById('fileSize').textContent = (file.size / 1024 / 1024).toFixed(2) + ' MB';
      } else {
        reader.readAsDataURL(file);
      }
      
      showToast('File selected: ' + file.name, 'info');
    }

    function removeFile() {
      selectedFile = null;
      document.getElementById('previewContainer').classList.add('hidden');
      document.getElementById('fileInput').value = '';
      document.getElementById('uploadStatus').innerHTML = '';
      document.getElementById('uploadBtn').innerHTML = 'Upload & Continue <i class="fas fa-arrow-right"></i>';
      // Clear wizard upload state
      wizard.data.nationalId.uploaded = false;
      wizard.data.nationalId.fileUrl = null;
      wizard.save();
    }

    function uploadAndNext() {
      // Check if already uploaded
      if (wizard.data.nationalId.uploaded && !selectedFile) {
        // Already uploaded, just continue
        window.location.href = '/pages/customer/apply-step4.html';
        return;
      }

      // Validate ID number
      const idNumber = document.getElementById('idNumber').value.trim();
      if (!idNumber) {
        document.getElementById('idError').classList.add('show');
        return;
      }
      document.getElementById('idError').classList.remove('show');

      // Validate file
      if (!selectedFile) {
        showToast('Please select a file to upload.', 'error');
        return;
      }

      // Start upload
      if (isUploading) return;
      isUploading = true;
      
      const btn = document.getElementById('uploadBtn');
      btn.disabled = true;
      btn.innerHTML = 'Uploading... <i class="fas fa-spinner fa-spin"></i>';

      const progressBar = document.getElementById('progressBar');
      const progressFill = document.getElementById('progressFill');
      progressBar.classList.add('active');

      // Simulate upload progress
      let progress = 0;
      const interval = setInterval(() => {
        progress += 8;
        if (progress <= 90) {
          progressFill.style.width = progress + '%';
        }
      }, 100);

      // Simulate upload completion
      setTimeout(() => {
        clearInterval(interval);
        progressFill.style.width = '100%';
        
        // Store the file data in wizard - CRITICAL: Set uploaded flag to true
        const fileUrl = URL.createObjectURL(selectedFile);
        wizard.setNationalId(idNumber, selectedFile, fileUrl);
        wizard.setNationalIdUploaded('doc_' + Date.now());
        
        // Update UI
        document.getElementById('uploadStatus').innerHTML = 
          '<span style="color:var(--success);font-weight:500;">✅ Upload successful!</span>';
        
        btn.innerHTML = 'Continue <i class="fas fa-arrow-right"></i>';
        btn.disabled = false;
        isUploading = false;
        
        showToast('ID uploaded successfully!', 'success');
        
        // Navigate to next step
        setTimeout(() => {
          window.location.href = '/pages/customer/apply-step4.html';
        }, 800);
        
      }, 2000);
    }

    // Allow Enter key on ID number field
    document.getElementById('idNumber').addEventListener('keypress', (e) => {
      if (e.key === 'Enter') {
        uploadAndNext();
      }
    });
  </script>
</body>
</html>

=========================================
FILE: pages/customer/apply-step4.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Step 4 - Review & Submit - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/apply-step3.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">Apply for Account</h1>
    </div>

    <!-- Stepper -->
    <div class="stepper">
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Account Type</span></div>
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Personal Details</span></div>
      <div class="step completed"><div class="circle"><i class="fas fa-check"></i></div><span class="label">Upload ID</span></div>
      <div class="step active"><div class="circle">4</div><span class="label">Review & Submit</span></div>
    </div>

    <div class="card">
      <h2>Review Your Application</h2>
      <p class="text-muted">Please review all details before submitting.</p>

      <div id="reviewContent" class="mt-2"></div>

      <!-- Debug info - remove after testing -->
      <div id="debugInfo" style="background:#f0f0f0;padding:10px;border-radius:6px;margin:10px 0;font-size:12px;display:none;"></div>

      <div class="input-group mt-2">
        <label>
          <input type="checkbox" id="termsCheckbox" onchange="toggleSubmit()" />
          I confirm that all information provided is accurate and complete.
        </label>
      </div>

      <div style="display:flex;justify-content:space-between;margin-top:var(--space);">
        <a href="/pages/customer/apply-step3.html" class="btn btn-secondary">
          <i class="fas fa-arrow-left"></i> Back
        </a>
        <button id="submitBtn" class="btn btn-gold" disabled onclick="submitApplication()">
          <i class="fas fa-paper-plane"></i> Submit Application
        </button>
      </div>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      wizard.load();
      renderReview();
      
      // Check if all data is complete and enable submit if terms were checked
      if (wizard.data.termsAccepted) {
        document.getElementById('termsCheckbox').checked = true;
        document.getElementById('submitBtn').disabled = false;
      }
    });

    function renderReview() {
      const data = wizard.data;
      const d = data.personalDetails || {};
      const id = data.nationalId || {};

      let html = `
        <table style="width:100%;border-collapse:collapse;">
          <tr><td style="padding:6px 0;"><strong>Account Type</strong></td><td style="padding:6px 0;">${data.accountType || 'Not selected'}</td></tr>
          <tr><td style="padding:6px 0;"><strong>Address</strong></td><td style="padding:6px 0;">${d.address || '—'}</td></tr>
          <tr><td style="padding:6px 0;"><strong>Occupation</strong></td><td style="padding:6px 0;">${d.occupation || '—'}</td></tr>
          <tr><td style="padding:6px 0;"><strong>Income Range</strong></td><td style="padding:6px 0;">${d.incomeRange || '—'}</td></tr>
          <tr><td style="padding:6px 0;"><strong>Next of Kin</strong></td><td style="padding:6px 0;">${d.nextOfKin || '—'} ${d.nextOfKinPhone ? '('+d.nextOfKinPhone+')' : ''}</td></tr>
          <tr><td style="padding:6px 0;"><strong>National ID Number</strong></td><td style="padding:6px 0;">${id.number || '—'}</td></tr>
          <tr><td style="padding:6px 0;"><strong>ID Uploaded</strong></td><td style="padding:6px 0;">${id.uploaded ? '✅ Yes' : '❌ No'}</td></tr>
        </table>
      `;

      if (id.fileUrl) {
        html += `<div class="mt-2"><img src="${id.fileUrl}" style="max-width:150px;border-radius:6px;border:1px solid var(--gray-200);" /></div>`;
      }

      document.getElementById('reviewContent').innerHTML = html;
      
      // Debug - log the wizard data
      console.log('Wizard Data:', wizard.data);
      console.log('Is Complete:', wizard.isComplete());
    }

    function toggleSubmit() {
      const checked = document.getElementById('termsCheckbox').checked;
      document.getElementById('submitBtn').disabled = !checked;
      wizard.setTermsAccepted(checked);
    }

    function submitApplication() {
      const btn = document.getElementById('submitBtn');
      
      // Double check terms
      if (!document.getElementById('termsCheckbox').checked) {
        showToast('Please accept the terms and conditions.', 'error');
        return;
      }

      // Disable button and show loading
      btn.disabled = true;
      btn.innerHTML = 'Submitting... <i class="fas fa-spinner fa-spin"></i>';

      // Get all data
      const data = wizard.data;
      
      // Check if we have all required data
      if (!data.accountTypeId) {
        showToast('Please select an account type.', 'error');
        btn.disabled = false;
        btn.innerHTML = '<i class="fas fa-paper-plane"></i> Submit Application';
        return;
      }

      if (!data.personalDetails.address || !data.personalDetails.occupation) {
        showToast('Please complete your personal details.', 'error');
        btn.disabled = false;
        btn.innerHTML = '<i class="fas fa-paper-plane"></i> Submit Application';
        return;
      }

      if (!data.nationalId.number || !data.nationalId.uploaded) {
        showToast('Please upload your National ID.', 'error');
        btn.disabled = false;
        btn.innerHTML = '<i class="fas fa-paper-plane"></i> Submit Application';
        return;
      }

      // Simulate submission
      setTimeout(() => {
        const appId = 'APP-' + Date.now();
        wizard.setApplicationId(appId);
        
        // Store application in session
        const apps = getStorage('applications') || [];
        const newApp = {
          id: appId,
          accountType: data.accountType,
          accountTypeId: data.accountTypeId,
          address: data.personalDetails.address,
          occupation: data.personalDetails.occupation,
          incomeRange: data.personalDetails.incomeRange || '',
          nextOfKin: data.personalDetails.nextOfKin || '',
          nextOfKinPhone: data.personalDetails.nextOfKinPhone || '',
          nationalId: data.nationalId.number,
          documentId: data.nationalId.documentId || 'doc_' + Date.now(),
          status: 'pending',
          submittedDate: new Date().toISOString(),
          bank: getSelectedBank()?.name || 'Unknown Bank',
          bankId: getSelectedBank()?.id || 'unknown'
        };
        apps.push(newApp);
        setStorage('applications', apps);

        // Reset wizard
        wizard.reset();

        showToast('🎉 Application submitted successfully!', 'success');
        setTimeout(() => {
          window.location.href = '/pages/customer/dashboard.html';
        }, 1500);
      }, 1500);
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/dashboard.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Dashboard - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: var(--space) 0;
      border-bottom: 1px solid var(--gray-200);
      margin-bottom: var(--space);
      flex-wrap: wrap;
      gap: var(--space);
    }
    .header .logo {
      font-family: 'Poppins', sans-serif;
      font-size: 1.5rem;
      font-weight: 700;
      color: var(--primary);
    }
    .header .logo span { color: var(--accent); }
    .user-info {
      display: flex;
      align-items: center;
      gap: var(--space);
      flex-wrap: wrap;
    }
    .user-avatar {
      width: 40px;
      height: 40px;
      border-radius: 50%;
      background: var(--primary);
      color: white;
      display: flex;
      align-items: center;
      justify-content: center;
      font-weight: 600;
    }
    .status-icon { font-size: 2.5rem; color: var(--accent); }
    .quick-actions { display: flex; gap: var(--space); flex-wrap: wrap; }
    .help-box {
      background: var(--gray-50);
      border-radius: var(--radius);
      padding: var(--space);
      margin-top: var(--space);
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <div>
        <div class="logo">SWIFT<span>b</span></div>
        <span class="text-muted" style="font-size:0.8rem;">Online Account Opening</span>
      </div>
      <div class="user-info">
        <div class="user-avatar" id="userAvatar">D</div>
        <span id="userName" style="font-size:0.875rem;font-weight:500;"></span>
        <button class="btn btn-secondary btn-sm" onclick="logout()">
          <i class="fas fa-sign-out-alt"></i> Logout
        </button>
      </div>
    </div>

    <!-- Status Card -->
    <div class="card">
      <div style="display:flex;align-items:center;gap:var(--space);">
        <div class="status-icon"><i class="fas fa-file-invoice"></i></div>
        <div style="flex:1;">
          <div class="card-header">Your Application Status</div>
          <div id="statusContent">
            <p class="text-muted">Loading...</p>
          </div>
        </div>
      </div>
    </div>

    <!-- Quick Actions -->
    <div class="mt-3">
      <h3>Quick Actions</h3>
      <div class="quick-actions mt-2">
        <a href="/pages/customer/apply-step1.html" class="btn btn-gold">
          <i class="fas fa-plus"></i> Start Application
        </a>
        <a href="/pages/customer/application-status.html" class="btn btn-secondary">
          <i class="fas fa-search"></i> View Status
        </a>
        <a href="/pages/customer/profile.html" class="btn btn-secondary">
          <i class="fas fa-user"></i> My Profile
        </a>
      </div>
    </div>

    <!-- Help -->
    <div class="help-box">
      <h4><i class="fas fa-info-circle" style="color:var(--accent);"></i> Need Help?</h4>
      <p style="font-size:0.875rem;color:var(--gray-500);">
        Contact your selected bank's customer support for assistance with your application.
      </p>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script src="/js/api.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', () => {
      // Check if user is logged in
      const token = sessionStorage.getItem('token');
      const user = getUserData();
      
      if (!token || !user) {
        showToast('Please login first', 'warning');
        setTimeout(() => {
          window.location.href = '/loginPage.html';
        }, 1000);
        return;
      }

      // Display user info
      if (user && user.fullName) {
        document.getElementById('userName').textContent = user.fullName;
        document.getElementById('userAvatar').textContent = user.fullName.charAt(0).toUpperCase();
      }

      loadApplicationStatus();
    });

    async function loadApplicationStatus() {
      const content = document.getElementById('statusContent');
      
      try {
        const applications = await api.getMyApplications();
        
        if (!applications || applications.length === 0) {
          content.innerHTML = `
            <p class="text-muted">You haven't started an application yet.</p>
            <a href="/pages/customer/apply-step1.html" class="btn btn-gold btn-sm mt-2">
              Start Your Application
            </a>
          `;
          return;
        }

        const app = applications[0];
        const statusMap = {
          PENDING: 'Pending Review',
          APPROVED: 'Approved ✅',
          REJECTED: 'Rejected ❌',
          RESUBMISSION: 'Resubmission Requested'
        };
        const badgeClass = {
          PENDING: 'badge-pending',
          APPROVED: 'badge-approved',
          REJECTED: 'badge-rejected',
          RESUBMISSION: 'badge-resubmission'
        };

        content.innerHTML = `
          <div>
            <p><strong>Status:</strong> 
              <span class="badge ${badgeClass[app.status] || 'badge-pending'}">
                ${statusMap[app.status] || app.status}
              </span>
            </p>
            <p><strong>Account Type:</strong> ${app.accountType}</p>
            ${app.rejectionReason ? `<p><strong>Note:</strong> ${app.rejectionReason}</p>` : ''}
            ${app.remarks ? `<p><strong>Remarks:</strong> ${app.remarks}</p>` : ''}
          </div>
        `;
      } catch (error) {
        if (error.message.includes('Session expired')) {
          // Will redirect to login
        } else {
          content.innerHTML = `<p class="text-danger">Error loading status: ${error.message}</p>`;
        }
      }
    }

    function logout() {
      sessionStorage.removeItem('token');
      sessionStorage.removeItem('role');
      sessionStorage.removeItem('user');
      window.location.href = '/loginPage.html';
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/landingPage.html
=========================================
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, 
    initial-scale=1.0, maximum-scale=1.0, user-scalable=no">

    <!-- Google Font -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
        rel="stylesheet">

    <!-- CSS -->
    <link rel="stylesheet" href="../../css/landingPage.css">

    <title>SwiftBank</title>
</head>

<body>

    <!-- Navigation -->
    <header>
        <nav class="navbar">

            <div class="logo">
                🏦Swift<span>Bank</span>
            </div>

            <ul class="nav-links">
                <li><a href="#">Home</a></li>
                <li><a href="#feature">Features</a></li>
                <li><a href="#howItWorks">How It Works</a></li>
                <li><a href="">About</a></li>
                <li><a href="">FAQs</a></li>
                
                
            </ul>

            <div class="nav-buttons">
                <a href="../../pages/auth/loginPage.html" class="register-btn">Login</a>
            </div>

        </nav>
    </header>

    <!-- Hero Section -->
    <section class="hero">

        <div class="hero-text">
            
            <h1>Open Your Bank Account Anytime, Anywhere.</h1>
            
            <p>
                No need to vist the Bank, Save your transport, Just you and your Mobile phone or pc.
                Experience a faster, secure, and paperless way of opening
                your bank account in just a few minutes.
            </p>

            <div class="hero-buttons">
                <a href="#" class="primary-btn">Open an Account</a>
                <a href="#" class="secondary-btn">Learn More</a>
            </div>

        </div>

        <div class="hero-image">
            <div class="slider">
                <div class="slide-track">
                    <img src="../../assets/images/N1.jpg">
                    <img src="../../assets/images/Nbs1.jpg" alt="">
                    <img src="../../assets/images/F1.jpg" alt="">
                    <img src="../../assets/images/Fd1.jpg" alt="">
                    <img src="../../assets/images/S1.jpg" alt="">

                    <img src="../../assets/images/N2.jpg" alt="">
                    <img src="../../assets/images/Nbs2.jpg" alt="">
                    <img src="../../assets/images/F2.jpg" alt="">
                    <img src="../../assets/images/Fd2.jpg" alt="">
                    <img src="../../assets/images/S2.jpg" alt="">

                    <img src="../../assets/images/N3.jpg" alt="">
                    <img src="../../assets/images/Nbs3.jpg" alt="">
                    <img src="../../assets/images/F3.jpg" alt="">
                    <img src="../../assets/images/Fd3.jpg" alt="">
                    <img src="../../assets/images/S3.jpg" alt="">

                    <img src="../../assets/images/N4.jpg" alt="">
                    <img src="../../assets/images/Nbs4.jpg" alt="">
                    <img src="../../assets/images/F4.jpg" alt="">
                    <img src="../../assets/images/Fd4.jpg" alt="">
                    <img src="../../assets/images/S4.jpg" alt="">

                    <img src="../../assets/images/N5.jpg" alt="">
                    <img src="../../assets/images/Nbs5.jpg" alt="">
                    <img src="../../assets/images/F5.jpg" alt="">
                    <img src="../../assets/images/Fd5.jpg" alt="">
                    <img src="../../assets/images/S5.jpg" alt="">

                    <!-- Duplicate-->

                     <img src="../../assets/images/N1.jpg">
                    <img src="../../assets/images/Nbs1.jpg" alt="">
                    <img src="../../assets/images/F1.jpg" alt="">
                    <img src="../../assets/images/Fd1.jpg" alt="">
                    <img src="../../assets/images/S1.jpg" alt="">

                    <img src="../../assets/images/N2.jpg" alt="">
                    <img src="../../assets/images/Nbs2.jpg" alt="">
                    <img src="../../assets/images/F2.jpg" alt="">
                    <img src="../../assets/images/Fd2.jpg" alt="">
                    <img src="../../assets/images/S2.jpg" alt="">

                    <img src="../../assets/images/N3.jpg" alt="">
                    <img src="../../assets/images/Nbs3.jpg" alt="">
                    <img src="../../assets/images/F3.jpg" alt="">
                    <img src="../../assets/images/Fd3.jpg" alt="">
                    <img src="../../assets/images/S3.jpg" alt="">

                    <img src="../../assets/images/N4.jpg" alt="">
                    <img src="../../assets/images/Nbs4.jpg" alt="">
                    <img src="../../assets/images/F4.jpg" alt="">
                    <img src="../../assets/images/Fd4.jpg" alt="">
                    <img src="../../assets/images/S4.jpg" alt="">

                    <img src="../../assets/images/N5.jpg" alt="">
                    <img src="../../assets/images/Nbs5.jpg" alt="">
                    <img src="../../assets/images/F5.jpg" alt="">
                    <img src="../../assets/images/Fd5.jpg" alt="">
                    <img src="../../assets/images/S5.jpg" alt="">

                    
                </div>
            </div>
        </div>

    </section>

    <!-- Features -->
    <section id="feature" class="features">

        <h2>Why Choose SwiftBank?</h2>

        <div class="feature-container">

            <div class="card">
                <h3>💻100% Online</h3>
                <p>Create your account without visiting a branch.</p>
            </div>

            <div class="card">
                <h3>🔏Secure</h3>
                <p>Your information is protected with modern security standards.</p>
            </div>

            <div class="card">
                <h3>🚀Fast Approval</h3>
                <p>Receive updates on your application in real time.</p>
            </div>

             <div class="card">
                <h3>⏱️24/7Access</h3>
                <p>Bank on your trems, anytime, anywhere</p>
            </div>

        </div>

    </section>

    <!-- How It Works -->
    <section id="howItWorks" class="steps">

        <h2>How It Works</h2>

        <div class="step-container">

            <div class="step">
                <h3>1</h3>
                <p>Register</p>
            </div>

            <div class="step">
                <h3>2</h3>
                <p>Complete Application</p>
            </div>

            <div class="step">
                <h3>3</h3>
                <p>Upload National ID</p>
            </div>

            <div class="step">
                <h3>4</h3>
                <p>Track Status</p>
            </div>

        </div>

    </section>

    <!-- Footer -->
    <footer>

        <p>© 2026 🏦SwiftBank. All Rights Reserved.</p>

    </footer>

    <script src="../../js/landingPageJScript.js"></script>

</body>

</html>
=========================================
FILE: pages/customer/profile.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/dashboard.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">My Profile</h1>
    </div>

    <div class="card">
      <h2>Personal Information</h2>
      <p class="text-muted">Your details for the application</p>

      <form id="profileForm" class="mt-2" onsubmit="saveProfile(event)">
        <div class="input-group">
          <label>Full Name *</label>
          <input type="text" id="fullName" required />
        </div>
        <div class="input-group">
          <label>Email *</label>
          <input type="email" id="email" required />
        </div>
        <div class="input-group">
          <label>Phone Number *</label>
          <input type="tel" id="phone" required />
        </div>
        <div class="input-group">
          <label>Date of Birth</label>
          <input type="date" id="dob" />
        </div>
        <div class="input-group">
          <label>Address</label>
          <input type="text" id="address" />
        </div>
        <div class="input-group">
          <label>Occupation</label>
          <input type="text" id="occupation" />
        </div>
        <div class="input-group">
          <label>National ID Number</label>
          <input type="text" id="nationalId" />
        </div>
        <div style="display:flex;gap:var(--space);">
          <button type="submit" class="btn btn-gold">Save Profile</button>
          <a href="/pages/customer/dashboard.html" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script src="/js/api.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', async () => {
      await loadProfile();
    });

    async function loadProfile() {
      try {
        const user = getUserData();
        if (user) {
          document.getElementById('fullName').value = user.fullName || '';
          document.getElementById('email').value = user.email || '';
          document.getElementById('phone').value = user.phoneNumber || '';
        }

        const profile = await api.getProfile();
        if (profile) {
          document.getElementById('fullName').value = profile.user?.fullName || user?.fullName || '';
          document.getElementById('email').value = profile.user?.email || user?.email || '';
          document.getElementById('phone').value = profile.user?.phoneNumber || user?.phoneNumber || '';
          document.getElementById('dob').value = profile.dateOfBirth || '';
          document.getElementById('address').value = profile.address || '';
          document.getElementById('occupation').value = profile.occupation || '';
          document.getElementById('nationalId').value = profile.nationalId || '';
        }
      } catch (error) {
        console.error('Error loading profile:', error);
        showToast('Failed to load profile', 'error');
      }
    }

    async function saveProfile(e) {
      e.preventDefault();
      
      const data = {
        dateOfBirth: document.getElementById('dob').value,
        address: document.getElementById('address').value,
        occupation: document.getElementById('occupation').value,
        nationalId: document.getElementById('nationalId').value,
      };

      try {
        await api.updateProfile(data);
        showToast('Profile updated successfully!', 'success');
      } catch (error) {
        showToast('Failed to update profile: ' + error.message, 'error');
      }
    }
  </script>
</body>
</html>

=========================================
FILE: pages/customer/profile.html
=========================================
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Profile - SWIFTb</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&family=Poppins:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/style.css" />
</head>
<body>
  <div class="container" style="max-width:800px;">
    <div style="display:flex;align-items:center;gap:var(--space);padding:var(--space) 0;border-bottom:1px solid var(--gray-200);margin-bottom:var(--space);">
      <a href="/pages/customer/dashboard.html" style="font-size:1.5rem;color:var(--primary);">
        <i class="fas fa-arrow-left"></i>
      </a>
      <h1 style="margin:0;">My Profile</h1>
    </div>

    <div class="card">
      <h2>Personal Information</h2>
      <p class="text-muted">Your details for the application</p>

      <form id="profileForm" class="mt-2" onsubmit="saveProfile(event)">
        <div class="input-group">
          <label>Full Name *</label>
          <input type="text" id="fullName" required />
        </div>
        <div class="input-group">
          <label>Email *</label>
          <input type="email" id="email" required />
        </div>
        <div class="input-group">
          <label>Phone Number *</label>
          <input type="tel" id="phone" required />
        </div>
        <div class="input-group">
          <label>Date of Birth</label>
          <input type="date" id="dob" />
        </div>
        <div class="input-group">
          <label>Address</label>
          <input type="text" id="address" />
        </div>
        <div class="input-group">
          <label>Occupation</label>
          <input type="text" id="occupation" />
        </div>
        <div class="input-group">
          <label>National ID Number</label>
          <input type="text" id="nationalId" />
        </div>
        <div style="display:flex;gap:var(--space);">
          <button type="submit" class="btn btn-gold">Save Profile</button>
          <a href="/pages/customer/dashboard.html" class="btn btn-secondary">Cancel</a>
        </div>
      </form>
    </div>
  </div>

  <script src="/js/app.js"></script>
  <script src="/js/api.js"></script>
  <script>
    document.addEventListener('DOMContentLoaded', async () => {
      await loadProfile();
    });

    async function loadProfile() {
      try {
        const user = getUserData();
        if (user) {
          document.getElementById('fullName').value = user.fullName || '';
          document.getElementById('email').value = user.email || '';
          document.getElementById('phone').value = user.phoneNumber || '';
        }

        const profile = await api.getProfile();
        if (profile) {
          document.getElementById('fullName').value = profile.user?.fullName || user?.fullName || '';
          document.getElementById('email').value = profile.user?.email || user?.email || '';
          document.getElementById('phone').value = profile.user?.phoneNumber || user?.phoneNumber || '';
          document.getElementById('dob').value = profile.dateOfBirth || '';
          document.getElementById('address').value = profile.address || '';
          document.getElementById('occupation').value = profile.occupation || '';
          document.getElementById('nationalId').value = profile.nationalId || '';
        }
      } catch (error) {
        console.error('Error loading profile:', error);
        showToast('Failed to load profile', 'error');
      }
    }

    async function saveProfile(e) {
      e.preventDefault();
      
      const data = {
        dateOfBirth: document.getElementById('dob').value,
        address: document.getElementById('address').value,
        occupation: document.getElementById('occupation').value,
        nationalId: document.getElementById('nationalId').value,
      };

      try {
        await api.updateProfile(data);
        showToast('Profile updated successfully!', 'success');
      } catch (error) {
        showToast('Failed to update profile: ' + error.message, 'error');
      }
    }
  </script>
</body>
</html>

=========================================
END OF FRONTEND CODE
=========================================
